{
      "name": "fraud-detection-backend",
      "version": "1.0.0",
      "description": "Backend for Fraud Detection System",
      "main": "App.js",
      "scripts": {
            "start": "node App.js",
            "dev": "nodemon App.js",
            "migrate": "node scripts/create-tables.js",
            "seed": "node scripts/seed-data.js",
            "setup-db": "npm run migrate && npm run seed",
            "test": "echo \"Error: no test specified\" && exit 1",
            "setup-malawi": "node scripts/setupMalawiData.js",
            "update-malawi": "node scripts/updateMalawiTransactions.js",
            "setup-recovery": "node scripts/setup-credential-recovery.js",
            "test-pdf": "node scripts/test-pdf-generation.js",
            "check-db": "node -e \"require('dotenv').config(); const { pool } = require('./config/db'); (async () => { try { const client = await pool.connect(); console.log('✅ Connected to database:', process.env.DB_DATABASE || process.env.DB_NAME); const tables = await client.query(\\\"SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'\\\"); console.log('📋 Tables:', tables.rows.map(r => r.table_name)); if (tables.rows.find(r => r.table_name === 'users')) { const users = await client.query('SELECT COUNT(*) FROM users'); console.log('👥 Users:', users.rows[0].count); } if (tables.rows.find(r => r.table_name === 'transactions')) { const transactions = await client.query('SELECT COUNT(*) FROM transactions'); console.log('💰 Transactions:', transactions.rows[0].count); } if (tables.rows.find(r => r.table_name === 'anomalies')) { const anomalies = await client.query('SELECT COUNT(*) FROM anomalies'); console.log('🚨 Anomalies:', anomalies.rows[0].count); } client.release(); process.exit(0); } catch(e) { console.error('❌ Error:', e.message); process.exit(1); } })();\""
      },
      "keywords": [],
      "author": "",
      "license": "ISC",
      "dependencies": {
            "axios": "^1.10.0",
            "bcrypt": "^6.0.0",
            "bcryptjs": "^2.4.3",
            "cors": "^2.8.5",
            "csv-parser": "^3.0.0",
            "docx": "^8.5.0",
            "dotenv": "^16.4.5",
            "exceljs": "^4.4.0",
            "express": "^4.19.2",
            "express-rate-limit": "^8.1.0",
            "faker": "^5.5.3",
            "helmet": "^7.1.0",
            "json2csv": "^6.0.0-alpha.2",
            "jsonwebtoken": "^9.0.2",
            "morgan": "^1.10.0",
            "multer": "^1.4.5-lts.1",
            "nodemailer": "^7.0.9",
            "pdfkit": "^0.15.2",
            "pg": "^8.12.0",
            "pg-format": "^1.0.4",
            "pino": "^10.1.0",
            "pino-http": "^11.0.0",
            "prom-client": "^13.2.0",
            "redis": "^4.6.15",
            "sequelize": "^6.37.7",
            "socket.io": "^4.7.5",
            "swagger-jsdoc": "^6.2.8",
            "swagger-ui-express": "^5.0.0",
            "uuid": "^11.1.0",
            "ws": "^8.18.3"
      },
      "devDependencies": {
            "nodemon": "^3.1.4",
            "pino-pretty": "^13.1.2"
      }
}
